<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FacteSimchin" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowSoftInputMode">adjustResize</item>
        <item name="android:fontFamily">@font/b_nazanin</item>
    </style>

    <style name="Theme.FacteSimchin" parent="Base.Theme.FacteSimchin">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:background">@drawable/bg_gradient</item>
    </style>

    <style name="AppTheme.Picker" parent="Theme.AppCompat.Light.NoActionBar" >
        <item name="android:textColorPrimary">@android:color/white</item>
    </style>

    <style name="MyAlertDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:textColorPrimary">#FFFFFF</item> <!-- Title & message text -->
        <item name="android:textColor">#00BCD4</item> <!-- Button text -->
        <item name="android:background">#7A7A7A</item>
    </style>
</resources>